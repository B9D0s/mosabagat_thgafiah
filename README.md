# 🏆 مسابقات السلطان - نظام بنك الأسئلة العربية

نظام متكامل لإدارة الأسئلة العربية للمسابقات الثقافية والتعليمية، يشمل بنك أسئلة منظم وAPI جاهز للربط مع ألعاب HTML.

---

## 📋 المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [المتطلبات](#-المتطلبات)
- [التثبيت والتشغيل](#-التثبيت-والتشغيل)
- [بنية المشروع](#-بنية-المشروع)
- [الـ API](#-الـ-api)
- [بنك الأسئلة](#-بنك-الأسئلة)
- [إضافة أسئلة جديدة](#-إضافة-أسئلة-جديدة)
- [نظام النقاط](#-نظام-النقاط)
- [ربط الـ API مع ألعاب HTML](#-ربط-الـ-api-مع-ألعاب-html)
- [التطبيقات المتوفرة](#-التطبيقات-المتوفرة)
- [استخراج محتوى من PDF](#-استخراج-محتوى-من-pdf)
- [قاعدة البيانات](#-قاعدة-البيانات)

---

## 🔍 نظرة عامة

| العنصر | التفاصيل |
|--------|----------|
| **عدد الأسئلة** | 585 سؤال (قابل للزيادة) |
| **أنواع الأسئلة** | اختيار من متعدد (MCQ): 419 + صح/خطأ (TF): 166 |
| **الفئات** | 13 فئة (القرآن، السيرة، الفقه، الحديث، الصحابة، العقيدة، التاريخ، الثقافة العامة...) |
| **مستويات الصعوبة** | easy / medium / hard / extreme |
| **التقنيات** | Node.js + Express + SQLite (sql.js) |
| **الـ API** | REST API على المنفذ 3000 |

---

## 📦 المتطلبات

- **Node.js** الإصدار 18 أو أعلى
- **npm** (يأتي مع Node.js)
- **Python 3** + مكتبة `pymupdf` (اختياري - لاستخراج PDF فقط)

---

## 🚀 التثبيت والتشغيل

### 1. تثبيت المكتبات

```bash
npm install
```

### 2. إدخال الأسئلة في قاعدة البيانات

```bash
npm run seed
```

هذا الأمر يقرأ `data/questions.json` ويدخل الأسئلة في قاعدة SQLite بـ `data/quiz.db`.

### 3. تشغيل السيرفر

```bash
# تشغيل عادي
npm start

# تشغيل مع إعادة تشغيل تلقائي عند التعديل
npm run dev
```

السيرفر يعمل على: **http://localhost:3000**

### 4. اختبار سريع

افتح المتصفح على:
- http://localhost:3000/health - فحص الحالة
- http://localhost:3000/api - توثيق الـ API
- http://localhost:3000/api/stats - إحصائيات بنك الأسئلة
- http://localhost:3000/api/questions/random?count=3 - 3 أسئلة عشوائية

---

## 📂 بنية المشروع

```
السلطان/
│
├── content/
│   └── book_clean.md              # مصدر المحتوى (Markdown)
│
├── data/
│   ├── questions.json             # بنك الأسئلة بصيغة JSON
│   └── quiz.db                    # قاعدة بيانات SQLite (تُنشأ تلقائياً)
│
├── seed/
│   └── seed_questions.js          # سكربت إدخال الأسئلة في القاعدة
│
├── src/
│   ├── server.js                  # السيرفر الرئيسي (Express)
│   ├── db/
│   │   └── database.js            # إدارة قاعدة البيانات SQLite
│   ├── routes/
│   │   └── questions.js           # نقاط وصول الـ API
│   ├── services/
│   │   ├── questionService.js     # خدمة الاستعلام عن الأسئلة
│   │   ├── ingest.js              # تحويل Markdown إلى JSON
│   │   └── extract_pdf.js         # استخراج نص من PDF
│   └── validators/
│       └── questionValidator.js   # التحقق من صحة الأسئلة
│
├── 7rof.html                      # لعبة مسابقة الألغام
├── million-game.html              # لعبة الأسر (المليون)
├── mzadat.html                    # البرنامج التحفيزي
├── matching-cards.html            # لعبة مطابقة البطاقات
├── true-false-game.html           # لعبة صح وخطأ
├── Schedule_Arrangment.html       # منصة تخطيط الأسبوع
│
├── package.json
└── README.md
```

---

## 🌐 الـ API

### نقاط الوصول (Endpoints)

#### `GET /health`

فحص حالة السيرفر.

```json
{
  "status": "ok",
  "name": "Sultan Quiz API - مسابقات السلطان",
  "version": "1.0.0"
}
```

---

#### `GET /api/categories`

الحصول على جميع الفئات مع عدد الأسئلة في كل فئة.

**الرد:**

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name_ar": "القرآن الكريم",
      "icon": "📖",
      "question_count": 23,
      "mcq_count": 18,
      "tf_count": 5
    }
  ],
  "count": 10
}
```

---

#### `GET /api/questions/random`

الحصول على أسئلة عشوائية مع إمكانية التصفية.

**المعاملات (Query Params):**

| المعامل | الوصف | القيم | الافتراضي |
|---------|-------|-------|-----------|
| `type` | نوع السؤال | `mcq` / `tf` / `any` | `any` |
| `category` | الفئة بالعربي | `الصحابة` / `السيرة` / ... | الكل |
| `difficulty` | مستوى الصعوبة | `easy` / `medium` / `hard` / `extreme` | الكل |
| `count` | عدد الأسئلة | 1 - 50 | 5 |
| `exclude_ids` | استثناء أسئلة | أرقام مفصولة بفاصلة | لا شيء |
| `reveal` | إظهار الإجابة | `true` / `false` | `false` |

**أمثلة:**

```
# 5 أسئلة عشوائية
GET /api/questions/random

# 3 أسئلة اختيار من متعدد صعبة
GET /api/questions/random?type=mcq&difficulty=hard&count=3

# سؤالين من فئة الصحابة مع إظهار الإجابة
GET /api/questions/random?category=الصحابة&count=2&reveal=true

# 10 أسئلة صح/خطأ سهلة مع استثناء أسئلة سبق عرضها
GET /api/questions/random?type=tf&difficulty=easy&count=10&exclude_ids=1,5,12
```

**رد بدون reveal:**

```json
{
  "success": true,
  "questions": [
    {
      "id": 1,
      "type": "mcq",
      "difficulty": "medium",
      "question_ar": "ما هي الغزوة التي حدثت فيها قصة الإفك؟",
      "category_ar": "السيرة",
      "category_icon": "📜",
      "points_base": 20,
      "options_ar": ["الخندق", "مؤتة", "دومة الجندل", "المريسيع"]
    }
  ],
  "total_available": 60,
  "returned": 1
}
```

**رد مع `reveal=true`:**

```json
{
  "questions": [
    {
      "id": 1,
      "type": "mcq",
      "question_ar": "ما هي الغزوة التي حدثت فيها قصة الإفك؟",
      "options_ar": ["الخندق", "مؤتة", "دومة الجندل", "المريسيع"],
      "correctIndex": 3,
      "source_ar": "مسابقات أهل القرآن - ص 6",
      "points_base": 20
    }
  ]
}
```

---

#### `POST /api/questions/verify`

التحقق من إجابة المستخدم وحساب النقاط.

**الطلب (Body):**

```json
{
  "question_id": 1,
  "answer": 3
}
```

- لأسئلة MCQ: الـ `answer` هو رقم الخيار (index يبدأ من 0)
- لأسئلة TF: الـ `answer` هو `true` أو `false`

**الرد:**

```json
{
  "success": true,
  "data": {
    "question_id": 1,
    "is_correct": true,
    "correct_answer": {
      "index": 3,
      "text": "المريسيع"
    },
    "points_earned": 20,
    "points_base": 20,
    "difficulty": "medium"
  }
}
```

---

#### `GET /api/stats`

إحصائيات شاملة عن بنك الأسئلة.

```json
{
  "success": true,
  "data": {
    "total_questions": 84,
    "by_type": { "mcq": 60, "tf": 24 },
    "by_difficulty": { "easy": 28, "medium": 35, "hard": 18, "extreme": 3 },
    "by_category": [
      { "name_ar": "القرآن الكريم", "icon": "📖", "count": 23 }
    ]
  }
}
```

---

## 📚 بنك الأسئلة

### الفئات المتوفرة

| الفئة | الأيقونة | عدد الأسئلة |
|-------|---------|------------|
| القرآن الكريم | 📖 | 23 |
| السيرة | 📜 | 11 |
| الثقافة العامة | 📚 | 11 |
| الفقه | ⚖️ | 9 |
| الصحابة | 🌟 | 8 |
| التاريخ الإسلامي | 🏛️ | 7 |
| الحديث الشريف | 📿 | 5 |
| العقيدة | 🕌 | 4 |
| الجغرافيا | 🌍 | 3 |
| اللغة العربية | ✍️ | 3 |

### صيغة السؤال (JSON)

**سؤال اختيار من متعدد:**

```json
{
  "type": "mcq",
  "category": "الصحابة",
  "difficulty": "medium",
  "question_ar": "من هو أول الخلفاء الراشدين؟",
  "options_ar": [
    "عمر بن الخطاب",
    "عثمان بن عفان",
    "أبو بكر الصديق",
    "علي بن أبي طالب"
  ],
  "correctIndex": 2,
  "source_ar": "معلومة عامة"
}
```

**سؤال صح / خطأ:**

```json
{
  "type": "tf",
  "category": "الثقافة العامة",
  "difficulty": "easy",
  "question_ar": "الرياض هي عاصمة المملكة العربية السعودية.",
  "correctBoolean": true,
  "source_ar": "معلومة عامة"
}
```

---

## ➕ إضافة أسئلة جديدة

### الطريقة 1: إضافة مباشرة في JSON

1. افتح `data/questions.json`
2. أضف الأسئلة الجديدة بالصيغة الموضحة أعلاه
3. شغل الإدخال:

```bash
npm run seed
```

الأسئلة المكررة يتم تخطيها تلقائياً.

### الطريقة 2: من ملف Markdown

1. اكتب الأسئلة في `content/book_clean.md` بالتنسيق:

```markdown
س1: ما هي أطول سورة في القرآن؟
أ- آل عمران
ب- النساء
ج- البقرة
د- المائدة
الجواب: ج

س2: النبي صلى الله عليه وسلم ولد في مكة - صح
```

2. شغل الاستخراج:

```bash
npm run ingest
```

3. ثم أدخل الأسئلة المستخرجة:

```bash
node seed/seed_questions.js data/questions_ingested.json
```

### الطريقة 3: من ملف JSON مخصص

```bash
node seed/seed_questions.js path/to/my_questions.json
```

---

## 🎯 نظام النقاط

كل سؤال يحمل نقاطاً حسب مستوى الصعوبة:

| المستوى | النقاط | الوصف |
|---------|--------|-------|
| `easy` 🟢 | 10 | معلومة مشهورة ومعروفة |
| `medium` 🟡 | 20 | تحتاج بعض المعرفة |
| `hard` 🔴 | 30 | تفاصيل دقيقة |
| `extreme` 💀 | 50 | نادرة أو دقيقة جداً |

---

## 🎮 ربط الـ API مع ألعاب HTML

### مثال: جلب أسئلة عشوائية

```javascript
async function fetchQuestions(type = 'mcq', count = 5, difficulty = '') {
  let url = `/api/questions/random?type=${type}&count=${count}`;
  if (difficulty) url += `&difficulty=${difficulty}`;

  const res = await fetch(url);
  const data = await res.json();
  return data.questions;
}

// استخدام
const questions = await fetchQuestions('mcq', 10, 'medium');
```

### مثال: التحقق من إجابة

```javascript
async function checkAnswer(questionId, answer) {
  const res = await fetch('/api/questions/verify', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      question_id: questionId,
      answer: answer
    })
  });
  const data = await res.json();
  return data.data;
  // { is_correct: true, points_earned: 20, correct_answer: {...} }
}
```

### مثال: جلب الفئات لعرضها كأزرار اختيار

```javascript
async function loadCategories() {
  const res = await fetch('/api/categories');
  const data = await res.json();

  data.data.forEach(cat => {
    console.log(`${cat.icon} ${cat.name_ar} (${cat.question_count} سؤال)`);
  });
}
```

### مثال: مسابقة ستريك (Streak)

```javascript
let streak = 0;
let score = 0;
let usedIds = [];

async function nextQuestion() {
  const res = await fetch(
    `/api/questions/random?type=mcq&count=1&exclude_ids=${usedIds.join(',')}`
  );
  const data = await res.json();

  if (data.questions.length === 0) {
    alert('انتهت الأسئلة!');
    return null;
  }

  const q = data.questions[0];
  usedIds.push(q.id);
  return q;
}

async function submitAnswer(questionId, answerIndex) {
  const result = await checkAnswer(questionId, answerIndex);

  if (result.is_correct) {
    streak++;
    // مضاعفة النقاط حسب الستريك
    const multiplier = Math.min(streak, 5);
    score += result.points_earned * multiplier;
  } else {
    streak = 0;
  }

  return { ...result, streak, score };
}
```

---

## 🖥️ التطبيقات المتوفرة

| الملف | الاسم | الوصف |
|-------|-------|-------|
| `million-game.html` | لعبة الأسر | مسابقة فرق مع نظام محفظة وبنك |
| `mzadat.html` | البرنامج التحفيزي | نظام تحفيز طلاب مع مزادات |
| `7rof.html` | مسابقة الألغام | لعبة شبكة تنافسية للمجموعات |
| `matching-cards.html` | مطابقة البطاقات | لعبة مطابقة أزواج |
| `true-false-game.html` | صح وخطأ | لعبة صح وخطأ |
| `Schedule_Arrangment.html` | تخطيط الأسبوع | منصة جدولة مع تصدير PDF |

لفتح أي لعبة: افتح ملف HTML مباشرة في المتصفح، أو عبر السيرفر:

```
http://localhost:3000/million-game.html
```

---

## 📄 استخراج محتوى من PDF

إذا عندك كتاب PDF وتبي تستخرج منه الأسئلة:

### المتطلبات

```bash
pip install pymupdf
```

### الخطوات

```bash
# 1. استخراج النص من PDF إلى Markdown
node src/services/extract_pdf.js "path/to/book.pdf" "content/book_clean.md"

# 2. تنظيف الملف يدوياً (مهم!)
# افتح content/book_clean.md ونظف النص

# 3. تحويل Markdown إلى JSON
npm run ingest

# 4. إدخال الأسئلة في قاعدة البيانات
node seed/seed_questions.js data/questions_ingested.json
```

### ملاحظة عن الكتب الحالية

- **مسابقات أهل القرآن** (110 صفحة): النص المستخرج كان مقلوباً بسبب RTL، تم استخراج الأسئلة يدوياً
- **كتاب 10000 سؤال** (1023 صفحة): PDF مسح ضوئي (صور)، يحتاج OCR احترافي لاستخراج النص

---

## 🗄️ قاعدة البيانات

### الجداول

```
categories          questions              choices
├── id              ├── id                 ├── id
├── name_ar         ├── type (mcq/tf)      ├── question_id
├── icon            ├── category_id        ├── choice_text_ar
└── description_ar  ├── difficulty          ├── choice_index
                    ├── question_ar         └── is_correct
                    ├── correct_boolean
                    ├── source_ar
                    ├── explanation_ar
                    ├── points_base
                    └── is_active
```

### إعادة بناء القاعدة من الصفر

```bash
# حذف القاعدة القديمة
rm data/quiz.db

# إعادة الإدخال
npm run seed
```

---

## 🔧 الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `npm start` | تشغيل السيرفر |
| `npm run dev` | تشغيل مع إعادة تشغيل تلقائي |
| `npm run seed` | إدخال الأسئلة من `data/questions.json` |
| `npm run ingest` | تحويل `content/book_clean.md` إلى JSON |
| `npm run extract-pdf` | استخراج نص من PDF |

---

## 🤝 المساهمة

لإضافة أسئلة جديدة:
1. أضف أسئلتك في `data/questions.json` بالصيغة الموحدة
2. شغل `npm run seed`
3. تأكد أن الأسئلة ظهرت في `GET /api/stats`
