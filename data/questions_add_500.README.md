# ملف إضافة أسئلة جديدة (300 أو 500 أو أي عدد)

الملف المقصود لتجميع الأسئلة الجديدة:

- `data/questions_add_500_new.json` (**الملف المعتمد** — موجود في .gitignore فلا يُرفع)

📌 **برومبت Gemini للتصنيفات الجديدة:** راجع `data/PROMPT-GEMINI-NEW-CATEGORIES.md` لنسخ برومبت يركز على التصنيفات الجديدة ويولّد أسئلة صعبة جداً.

## التصنيفات الكاملة (17 فئة)

| الأساسية | التصنيفات الجديدة |
|----------|-------------------|
| القرآن الكريم، السيرة، الفقه، الحديث الشريف، الصحابة، العقيدة، التاريخ الإسلامي، الثقافة العامة، الجغرافيا، اللغة العربية، العلوم، الرياضيات، معلومات إسلامية | **الأحياء**، **علوم الفضاء**، **من القائل**، **أكمل الآية** |

**لإنشاء أسئلة إضافية في التصنيفات الجديدة فقط** (لإرسالها لـ Gemini):  
ركّز البرومبت على: **الأحياء، علوم الفضاء، الجغرافيا، من القائل، أكمل الآية** — راجع `PROMPT-GEMINI-NEW-CATEGORIES.md`.

## المطلوب داخل الملف

- لازم يكون **JSON Array فقط** بالشكل:

```json
[
  { ... سؤال 1 ... },
  { ... سؤال 2 ... }
]
```

- **ممنوع** أي نص خارج الـ JSON.
- لا تضف `id` — يُضاف تلقائياً عند الدمج.

## نوعين من الأسئلة

### 1) صح/خطأ (`tf`)

```json
{
  "type": "tf",
  "category": "من القائل",
  "difficulty": "extreme",
  "question_ar": "نص السؤال...",
  "correctBoolean": true,
  "source_ar": "اختياري"
}
```

### 2) اختيار من متعدد (`mcq`)

```json
{
  "type": "mcq",
  "category": "أكمل الآية",
  "difficulty": "hard",
  "question_ar": "نص السؤال...",
  "options_ar": ["خيار 1", "خيار 2", "خيار 3", "خيار 4"],
  "correctIndex": 2,
  "source_ar": "اختياري"
}
```

## دمج الأسئلة داخل الداتابيس الأساسية

```bash
# 1. فحص
node tests/merge-questions.js data/questions_add_500_new.json

# 2. تطبيق الدمج
node tests/merge-questions.js data/questions_add_500_new.json --apply

# 3. دمج + تفريغ الملف (جاهز للدفعة التالية)
node tests/merge-questions.js data/questions_add_500_new.json --apply --clear-after
```

السكربت راح:
- يدمج داخل `data/questions.json`
- يمنع التكرار حسب (`type` + `question_ar`)
- يرقّم كل الأسئلة تلقائياً (`id: 1, 2, 3, ...`)
- يطلع لك تقرير بعدد الأسئلة المضافة/المرفوضة

---

## آلية كتابة الأسئلة (إضافة يدوي أو توليد بـ AI)

### القواعد العامة
- الملف = **مصفوفة JSON فقط** `[ ... ]`
- لا تضف `id` — يُضاف تلقائياً عند الدمج
- `source_ar` اختياري (يمكن حذفه أو تركه فارغاً)

### التصنيفات المسموحة (17)
القرآن الكريم، السيرة، الفقه، الحديث الشريف، الصحابة، العقيدة، التاريخ الإسلامي، الثقافة العامة، الجغرافيا، اللغة العربية، العلوم، الرياضيات، معلومات إسلامية، **الأحياء**، **علوم الفضاء**، **من القائل**، **أكمل الآية**

### المستويات المسموحة
`"easy"` | `"medium"` | `"hard"` | `"extreme"`

### صح/خطأ — الحقول الإلزامية
| الحقل          | النوع     | ملاحظة                         |
|----------------|-----------|--------------------------------|
| type           | `"tf"`    | ثابت                          |
| category       | string    | من الـ 17 تصنيفاً أعلاه       |
| difficulty     | string    | من المستويات أعلاه             |
| question_ar    | string    | نص السؤال                     |
| correctBoolean | boolean   | `true` أو `false` بدون اقتباس  |

### اختيار من متعدد — الحقول الإلزامية
| الحقل        | النوع     | ملاحظة                                      |
|--------------|-----------|---------------------------------------------|
| type         | `"mcq"`   | ثابت                                       |
| category     | string    | من الـ 17 تصنيفاً                           |
| difficulty   | string    | من المستويات                               |
| question_ar   | string    | نص السؤال                                  |
| options_ar   | array     | **4 خيارات بالضبط** ولا تتكرر              |
| correctIndex | number    | 0 أو 1 أو 2 أو 3 (فهرس الإجابة الصحيحة)   |

### ما يتحمله السكربت
- مصفوفة داخل مصفوفة `[[ ... ]]` — يفتحها تلقائياً
- صيغ بديلة لـ type: `truefalse`, `multiple_choice` إلخ
- صيغ بديلة لـ difficulty: `"سهل"`, `"very hard"` → يُحوّل تلقائياً
- توحيد category: `General_Culture` → الثقافة العامة، `جغرافيا` → الجغرافيا إلخ

**توحيد الداتابيس فقط (بدون دمج):** `node tests/merge-questions.js --normalize --apply`
