# شرح قاعدة بيانات الأسئلة — مسابقات السلطان

## 1. ما هي "الداتابيس"؟

ليست قاعدة بيانات تقليدية (مثل MySQL). المصدر هو **ملف JSON واحد**:  
`data/questions.json`

- يُحمَّل عند فتح لعبة **صح وخطأ** (من CDN إذا وُجد إنترنت، أو من نسخة محلية).
- يُقرأ كاملاً إلى الذاكرة (مصفوفة JavaScript).
- **لا يُحفظ** أي تعديل أثناء اللعب؛ التعديل يكون دائماً على الملف نفسه.

---

## 2. هيكلة الملف (Structure)

الملف = **مصفوفة JSON** من كائنات (objects). كل كائن = سؤال واحد.

يوجد **نوعان** من الأسئلة حسب الحقل `type`:

### أ) أسئلة اختيار من متعدد — `"type": "mcq"`

تُستخدم في تطبيقات أخرى (مثل لعبة المليون إن وُجد). **لعبة صح وخطأ لا تستخدمها**؛ تستخدم فقط `tf`.

```json
{
  "type": "mcq",
  "category": "السيرة",
  "difficulty": "medium",
  "question_ar": "ما هي الغزوة التي حدثت فيها قصة الإفك؟",
  "options_ar": ["الخندق", "مؤتة", "دومة الجندل", "المريسيع"],
  "correctIndex": 3,
  "source_ar": "مسابقات أهل القرآن - ص 6"
}
```

| الحقل          | الوصف |
|----------------|--------|
| `type`         | يجب أن يكون `"mcq"` |
| `category`     | التصنيف الموضوعي (انظر التصانيف أدناه) |
| `difficulty`   | `"easy"` أو `"medium"` أو `"hard"` أو `"extreme"` |
| `question_ar`  | نص السؤال بالعربية |
| `options_ar`   | مصفوفة من 4 خيارات بالعربية |
| `correctIndex` | index الإجابة الصحيحة (0–3) |
| `source_ar`    | مصدر السؤال (اختياري) |

---

### ب) أسئلة صح وخطأ — `"type": "tf"`

هذه **الوحيدة** التي تستخدمها لعبة **صح وخطأ**.

```json
{
  "type": "tf",
  "category": "السيرة",
  "difficulty": "easy",
  "question_ar": "توفي الرسول صلى الله عليه وسلم يوم الاثنين ودفن يوم الثلاثاء.",
  "correctBoolean": false,
  "source_ar": "مسابقات أهل القرآن - ص 7"
}
```

| الحقل            | الوصف |
|------------------|--------|
| `type`           | يجب أن يكون `"tf"` |
| `category`       | التصنيف الموضوعي |
| `difficulty`     | `"easy"` أو `"medium"` أو `"hard"` أو `"extreme"` |
| `question_ar`    | نص السؤال بالعربية |
| `correctBoolean` | `true` = الإجابة "صحيح"، `false` = "خطأ" |
| `source_ar`      | مصدر السؤال (اختياري) |

---

## 3. التصانيف (Classifications)

### 3.1 تصنيف حسب **المحتوى** — `category`

التصنيفات الموحّدة الحالية في الداتابيس:

| التصنيف           | مثال استخدام |
|-------------------|----------------|
| القرآن الكريم     | أسئلة قرآنية |
| السيرة            | سيرة الرسول صلى الله عليه وسلم |
| الفقه             | أحكام الصلاة، الزكاة، الصيام... |
| الحديث الشريف     | أحاديث |
| الصحابة           | صحابة رسول الله |
| العقيدة           | عقيدة إسلامية |
| التاريخ الإسلامي  | أحداث وتواريخ |
| الثقافة العامة    | ثقافة عامة |
| الجغرافيا         | دول، عواصم، بحار... |
| اللغة العربية     | نحو، إملاء... |
| العلوم            | علوم عامة |
| الرياضيات         | رياضيات |
| معلومات إسلامية   | معلومات عامة في الإسلام |

- **لعبة صح وخطأ** لا تفلتر حالياً حسب `category`؛ كل أسئلة `tf` تدخل في الخلطة.
- يمكن لاحقاً إضافة فلتر "موضوع" في الإعدادات وربطه بـ `category`.

### 3.2 تصنيف حسب **الصعوبة** — `difficulty`

أربعة مستويات في الملف:

| القيمة في JSON | المعنى في اللعبة |
|----------------|-------------------|
| `"easy"`       | سهل 🟢 |
| `"medium"`     | متوسط 🟡 |
| `"hard"`       | صعب 🔴 |
| `"extreme"`    | صعب جداً 💀 |

- اللعبة تختار مستوى عام (سهل / متوسط / صعب / **صعب جداً**).
- حسب المستوى المختار، تُحدد **نسب** السهل والمتوسط والصعب في الـ 60 سؤال (انظر الجدول في القسم 4).

---

## 4. توزيع الصعوبة في لعبة صح وخطأ (60 سؤال)

المستوى العام يتحكم في **النسب** فقط، والأسئلة تُسحب من الداتابيس حسب `difficulty`:

| المستوى في اللعبة | سهل | متوسط | صعب |
|-------------------|-----|--------|-----|
| سهل 🟢            | 60% | 30%    | 10% |
| متوسط 🟡          | 20% | 55%    | 25% |
| صعب 🔴            | 10% | 25%    | 65% |
| صعب جداً 💀       | 5%  | 15%    | **80%** |

- عدد أسئلة **صح وخطأ** في الملف محدود (عشرات فقط من نوع `tf`).
- إذا لم يكفِ عدد الأسئلة **الصعبة** لتحقيق النسبة (مثلاً 80% صعب في "صعب جداً")، اللعبة **تعوّض** من المتوسط والسهل حتى تكمل 60 سؤالاً.
- لذلك كلما زادت أسئلة `tf` المصنفة `"hard"`، أصبح خيار "صعب جداً" أوضح وأقوى.

---

## 5. آلية إضافة أسئلة

### الخطوات

1. افتح الملف: **`data/questions.json`** (محرر نصوص أو VS Code / Cursor).
2. الملف = مصفوفة `[ ... ]`. ضع كل سؤال جديد كـ **كائن** داخل هذه المصفوفة (مع فاصلة بين الكائنات).
3. للعبة **صح وخطأ** أضف أسئلة من نوع **`tf`** فقط بالهيكل التالي.

### نموذج سؤال صح/خطأ جاهز للنسخ

```json
{
  "type": "tf",
  "category": "السيرة",
  "difficulty": "hard",
  "question_ar": "نص السؤال هنا بالعربية.",
  "correctBoolean": true,
  "source_ar": "المصدر (اختياري)"
}
```

- غيّر `category` و `difficulty` و `question_ar` و `correctBoolean` و `source_ar` حسب الحاجة.
- تأكد أن:
  - `difficulty` أحد: `"easy"` أو `"medium"` أو `"hard"`.
  - `correctBoolean` قيمة منطقية: `true` أو `false` بدون علامات اقتباس.

### أين أضيف السؤال؟

- في **أي مكان** داخل المصفوفة (بداية، وسط، نهاية).
- المهم أن تضع **فاصلة** بين السؤال الجديد والكائن الذي قبله أو بعده حتى يبقى الـ JSON صالحاً.

### بعد الحفظ

- إذا كانت اللعبة تأخذ الأسئلة من **CDN** (رابط GitHub)، يجب رفع الملف المحدّث إلى الريبو حتى يظهر التحديث أونلاين.
- إذا كانت تعمل **محلياً** (أوفلاين)، كفى حفظ الملف وإعادة تحميل الصفحة (F5).

---

## 6. ملخص سريع

| البند            | التفاصيل |
|------------------|----------|
| **المصدر**       | `data/questions.json` |
| **الهيكلة**      | مصفوفة من كائنات؛ كل كائن = سؤال |
| **للصح وخطأ**   | استخدم فقط أسئلة `"type": "tf"` |
| **التصانيف**    | `category` (موضوع) و `difficulty` (سهل/متوسط/صعب) |
| **إضافة أسئلة** | تعديل الملف يدوياً، إضافة كائن جديد بنفس الحقول |
| **التحديث**     | حفظ الملف + رفع للـ CDN إن لزم، أو F5 للاستخدام المحلي |

إذا أحببت، يمكن في خطوة لاحقة إضافة أمثلة لأسئلة `mcq` أو سكربت بسيط لعدّ أسئلة `tf` حسب `difficulty` من الملف.
